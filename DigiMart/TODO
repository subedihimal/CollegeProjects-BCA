Growth Rate: Fix the calculation or remove it if it is not meaningful.
Normalized MAE: Rename it to WAPE since 15.91% represents WAPE.
Rounding: Use consistent decimal places across all dashboard metrics.
MAPE: Show 17.0% on the dashboard and 16.97% in the report.
Currency: Display monetary values as whole dollars with comma separators, e.g. $28,621.
Metric calculation: Calculate all metrics using full-precision values and round only for display.
Duplicate metrics: Remove either Normalized MAE or WAPE after correcting the label, since they represent the same metric.
Fix the rounding in report or the webpage of metrics
Fix diagram sarima shows offline generation
Fix refrences and biblography
Implement the Recommendation test in the documentation in code
